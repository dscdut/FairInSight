// Deprecated intentionally.
// Lawyer list/detail data must come from the real backend APIs:
// - GET /api/v1/lawyers
// - GET /api/v1/lawyers/:id
// - GET /api/v1/lawyers/recommendations
//
// The previous MOCK_LAWYERS fixtures were removed to avoid accidental fallback
// to frontend-only lawyer data.
